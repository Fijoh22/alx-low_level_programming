/**
 * * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

	for (ch = 'A'; ch <= 'A'; ch++)
		putchar(ch):\;

	putchar('\n');

	return (0);
